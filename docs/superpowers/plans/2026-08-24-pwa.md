# PWA化・HTTPS配信 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 先生がホーム画面のアイコンから開き、電波の無い教室でも起動して使える状態にする。あわせて HTTPS 配信できるようにし、カメラを実機で試せるようにする。

**Architecture:** `vite-plugin-pwa` の `generateSW` に寄せる。**プリキャッシュにフォント実体を含めない**のが設計の中心で、初回約920KBに収める。フォントはブラウザのサブセット選択に任せ、実行時キャッシュで端末に残す。更新は自動適用せず、先生が押したときだけ切り替える。

**Tech Stack:** 既存に `vite-plugin-pwa`（1.3.0）と `sharp`（0.35.3、アイコン生成のみ・devDependency）を追加する。

設計書: [`docs/superpowers/specs/2026-08-24-pwa-design.md`](../specs/2026-08-24-pwa-design.md)

## Global Constraints

ステップ1〜3から引き継ぐ。全タスクの要件に暗黙に含まれる。

- **アプリのコードにネットワークアクセスを書かない。** `fetch`、CDN、外部URLは禁止。Service Worker がフォントを取りに行くのは同一オリジンへの要求であり、外部通信ではない。
- **UIの文言はすべて日本語。** エラー文は何が起きたかと次にどうするかを示す。謝罪表現は使わない。
- **配色トークンは5色のみ**: `gayoshi` / `sumi` / `ai` / `kogan` / `shu`。**Tailwind素のカラーを使ってはいけない。** 藍の上に載せる明色は `text-gayoshi`。
- **朱は花丸と「完全に削除」の確認ダイアログのみ。** 更新の帯には使わない（藍を使う）。
- 書体: 数値は `font-num`、見出しは `font-display`。**Klee One を本文に使わない。**
- スマホ幅375pxまで崩れない、キーボードフォーカスが見える、タップ領域は44px四方以上。
- TypeScriptは `strict: true`。`any` を使わない。`noUnusedLocals: true` なので未使用importでビルドが落ちる。
- **テストは非同期に変わる内容を1回だけ見て判定しない。** 現れる内容には `findBy*`、既存要素の属性には `waitFor` を使う。このプロジェクトはこの形の競合を6回踏んでいる。
- 各タスクの最後に必ずコミットする。

## 現状の前提（ステップ3完了時点）

- テスト267件が通っている。**終わったときも全部通っていること。**
- `npm run test:run`、`npm run build`（`tsc --noEmit && vite build`）
- `public/` ディレクトリは**まだ無い**
- `vite.config.ts` は `vitest/config` の `defineConfig` を使い、`react()` と `tailwindcss()` のみ
- `src/App.tsx` の catch-all は `/roster` を指している
- `src/main.tsx` はフォント4つと `styles/index.css` を読み、`<App />` を描画するだけ

## 実測済みの数値

計画作成時にビルドして測った値。実装後に変わっていたら方針を見直すこと。

| 対象 | 量 |
|---|---|
| JS | 417KB |
| CSS（496個の `@font-face` 宣言を含む） | 503KB |
| HTML | 1KB未満 |
| woff2（フォント実体、488ファイル） | 11.54MB |
| **合計** | **約25MB**（`dist` 全体） |

**プリキャッシュ目標は約920KB**（JS + CSS + HTML）。

---

### Task 1: ホームを `/scan` にする

PWA化の前に片付ける。`start_url` が指す先を先に決めておかないと、manifest を書くときに迷う。

**Files:**
- Modify: `src/App.tsx`
- Test: `src/screens/Home.test.tsx`（新規）

**Interfaces:**
- Produces: catch-all（`*`）の行き先が `/scan` になる

- [ ] **Step 1: 失敗するテストを書く**

`src/screens/Home.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { beforeEach, describe, expect, it } from "vitest";
import { MemoryRouter } from "react-router";
import { useFreshDb } from "../test/db";
import { AppRoutes } from "../App";
import { createCohort } from "../db/cohorts";
import { addSubmissionType } from "../db/submissionTypes";
import { formatDateHeading } from "../lib/date";

useFreshDb();

const TODAY = new Date();

let cohortId = "";

beforeEach(async () => {
  const cohort = await createCohort({ year: 2026, className: "5年1組" });
  cohortId = cohort.id;
});

function renderAt(path: string) {
  return render(
    <MemoryRouter initialEntries={[path]}>
      <AppRoutes />
    </MemoryRouter>,
  );
}

describe("知らないパスを開いたとき", () => {
  it("提出チェックへ送る", async () => {
    await addSubmissionType({
      cohortId,
      name: "計算ドリル",
      deadline: "08:15",
      weekdays: [TODAY.getDay()],
    });

    renderAt("/");

    expect(
      await screen.findByText(formatDateHeading(TODAY)),
    ).toBeInTheDocument();
  });

  it("提出物が無くても行き止まりにしない", async () => {
    // 分岐を入れない代わりに、スキャン画面が空の状態を引き受ける。
    renderAt("/");

    expect(
      await screen.findByText("まず提出物を登録してください"),
    ).toBeInTheDocument();
    expect(
      screen.getByRole("link", { name: "提出物の設定" }),
    ).toHaveAttribute("href", "/submissions");
  });
});
```

cohort が無いときの挙動は既存の `SetupGuard.test.tsx` と `Setup.test.tsx` が担保しているため、ここでは書かない。

- [ ] **Step 2: テストを実行して失敗を確認する**

Run: `npm run test:run -- src/screens/Home.test.tsx`
Expected: FAIL。catch-all が `/roster` を指しているため名簿が表示される。

- [ ] **Step 3: catch-all を変える**

`src/App.tsx`:

```tsx
      <Route path="*" element={<Navigate to="/scan" replace />} />
```

- [ ] **Step 4: テストを実行して成功を確認する**

Run: `npm run test:run -- src/screens/Home.test.tsx`
Expected: PASS（2件）

- [ ] **Step 5: 全テストと型チェックを通す**

Run: `npm run test:run && npm run build`
Expected: 269件すべてPASS、型エラーなし。

**既存テストが落ちる可能性がある。** `/roster` へ送られる前提で書かれたテストがあれば、`/scan` へ変わったことによる正当な失敗か、見落としかを判断すること。判断がつかなければ報告する。

- [ ] **Step 6: コミット**

```bash
git add -A
git commit -m "feat: ホームを提出チェックにする"
```

---

### Task 2: アイコンを作る

花丸のアイコン。生成は一度だけで、結果をコミットする。

**Files:**
- Create: `scripts/icon.svg`
- Create: `scripts/generate-icons.mjs`
- Create: `public/icon-192.png`、`public/icon-512.png`、`public/icon-maskable-512.png`、`public/apple-touch-icon.png`（生成物）
- Modify: `package.json`（`sharp` を devDependency に、スクリプトを追加）

**Interfaces:**
- Produces: `public/` に4つのPNG

- [ ] **Step 1: `sharp` を入れる**

Run: `npm install -D sharp@^0.35.3`

**`sharp` は devDependency にする。** アイコン生成にしか使わず、アプリの実行にもビルドにも関与しない。

- [ ] **Step 2: 元になるSVGを描く**

`scripts/icon.svg`。既存の `src/components/Hanamaru.tsx` と同じ線を使う。色は変数ではなく実値で書く（SVGを単体で `sharp` に渡すため）。

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="512" height="512">
  <!-- 藍地。ホーム画面に並んだとき他のアイコンに埋もれない -->
  <rect width="100" height="100" fill="#22406B"/>
  <!-- 花丸。Hanamaru.tsx と同じ path -->
  <g stroke="#D8452E" fill="none" stroke-width="5" stroke-linecap="round">
    <circle cx="50" cy="50" r="34"/>
    <path d="M50 28 C60 38 62 50 50 58 C38 50 40 38 50 28 Z
             M72 50 C62 60 50 62 42 50 C50 38 62 40 72 50 Z"/>
  </g>
</svg>
```

- [ ] **Step 3: maskable 用のSVGを分けずに余白で対応する**

maskable アイコンは端末が丸や角丸に切り抜くため、**外周20%が削られても図柄が残る**必要がある。同じSVGを縮小して中央に置く形で作る。生成スクリプト側で処理するので、SVGは1つでよい。

- [ ] **Step 4: 生成スクリプトを書く**

`scripts/generate-icons.mjs`:

```js
// アイコンを生成する。ビルドのたびに走らせる必要はない。
// 図柄を変えたときだけ `npm run icons` を実行し、結果をコミットする。
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
import sharp from "sharp";

const here = dirname(fileURLToPath(import.meta.url));
const publicDir = join(here, "..", "public");

const svg = await readFile(join(here, "icon.svg"));

await mkdir(publicDir, { recursive: true });

/** 通常のアイコン。図柄が縁まで届く。 */
async function plain(size, name) {
  const png = await sharp(svg).resize(size, size).png().toBuffer();
  await writeFile(join(publicDir, name), png);
  console.log(`${name} (${size}x${size})`);
}

/**
 * maskable。端末が外周を切り抜くため、図柄を80%に縮めて中央に置く。
 * 縮めないと花丸の縁が削られる。
 */
async function maskable(size, name) {
  const inner = Math.round(size * 0.8);
  const pad = Math.round((size - inner) / 2);

  const shrunk = await sharp(svg).resize(inner, inner).png().toBuffer();
  const png = await sharp({
    create: {
      width: size,
      height: size,
      channels: 4,
      // 切り抜かれる外周も藍で埋める
      background: { r: 0x22, g: 0x40, b: 0x6b, alpha: 1 },
    },
  })
    .composite([{ input: shrunk, top: pad, left: pad }])
    .png()
    .toBuffer();

  await writeFile(join(publicDir, name), png);
  console.log(`${name} (${size}x${size}, maskable)`);
}

await plain(192, "icon-192.png");
await plain(512, "icon-512.png");
await plain(180, "apple-touch-icon.png");
await maskable(512, "icon-maskable-512.png");
```

`package.json` の `scripts` に足す。

```json
    "icons": "node scripts/generate-icons.mjs",
```

- [ ] **Step 5: 実行して確認する**

Run: `npm run icons`
Expected: 4つのPNGが `public/` に作られる。

生成後、サイズを確認する。

Run: `ls -la public/*.png`
Expected: 4ファイル。512pxのものが数KB〜数十KB程度（単純な図形なので大きくならない）。

**目視確認が要る。** 画像が真っ黒・真っ白でないこと、花丸が中央にあることを確認する。ブラウザで `public/icon-512.png` を開けば見える。できない環境なら、報告書に「未確認」と明記すること。

- [ ] **Step 6: コミット**

```bash
git add -A
git commit -m "feat: 花丸のアイコンを追加"
```

---

### Task 3: PWA化

**Files:**
- Modify: `vite.config.ts`
- Create: `public/_redirects`
- Modify: `package.json`（`vite-plugin-pwa`）
- Test: `src/pwa/manifest.test.ts`（新規）

**Interfaces:**
- Produces:
  - `dist/manifest.webmanifest`
  - `dist/sw.js`
  - `dist/_redirects`

- [ ] **Step 1: `vite-plugin-pwa` を入れる**

Run: `npm install -D vite-plugin-pwa@^1.3.0`

- [ ] **Step 2: SPAの再読み込みに対応する**

`public/_redirects`:

```
# 静的ホストで /roster などを直接開いた・再読み込みしたときに
# index.html を返す。Cloudflare Pages と Netlify の両方がこの
# ファイルを読む。無いと 404 になる。
/*    /index.html   200
```

- [ ] **Step 3: manifest を定数に切り出す**

テストから読めるようにするため、設定値を別ファイルに置く。`vite.config.ts` に直接書くとテストから参照しにくい。

`src/pwa/manifest.ts`:

```ts
/**
 * ホーム画面に追加したときの見え方。
 *
 * vite.config.ts から読む。テストからも読めるよう別ファイルにしている。
 */
export const manifest = {
  name: "宿題提出管理",
  short_name: "宿題チェック",
  description: "宿題の提出をQRコードでチェックする",
  lang: "ja",
  start_url: "/",
  scope: "/",
  display: "standalone",
  orientation: "portrait",
  background_color: "#FBFAF7",
  theme_color: "#22406B",
  icons: [
    { src: "/icon-192.png", sizes: "192x192", type: "image/png" },
    { src: "/icon-512.png", sizes: "512x512", type: "image/png" },
    {
      src: "/icon-maskable-512.png",
      sizes: "512x512",
      type: "image/png",
      purpose: "maskable",
    },
  ],
} as const;
```

- [ ] **Step 4: manifest のテストを書く**

`src/pwa/manifest.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { manifest } from "./manifest";

describe("manifest", () => {
  it("ホーム画面から専用アプリとして開く", () => {
    expect(manifest.display).toBe("standalone");
  });

  it("起動先はホーム", () => {
    // catch-all が /scan へ送るので、/ で開けば提出チェックに着く
    expect(manifest.start_url).toBe("/");
  });

  it("日本語のアプリとして宣言する", () => {
    expect(manifest.lang).toBe("ja");
  });

  it("配色トークンの色を使う", () => {
    // 画用紙と藍。ここだけ別の色を使うとホーム画面で浮く
    expect(manifest.background_color).toBe("#FBFAF7");
    expect(manifest.theme_color).toBe("#22406B");
  });

  it("必要なサイズのアイコンが揃っている", () => {
    const sizes = manifest.icons.map((icon) => icon.sizes);
    expect(sizes).toContain("192x192");
    expect(sizes).toContain("512x512");
  });

  it("maskable のアイコンがある", () => {
    // 端末が丸や角丸に切り抜くため、専用の余白付きが要る
    const maskable = manifest.icons.filter(
      (icon) => "purpose" in icon && icon.purpose === "maskable",
    );
    expect(maskable).toHaveLength(1);
    expect(maskable[0].sizes).toBe("512x512");
  });

  it("short_name はホーム画面に収まる長さにする", () => {
    // 端末によっては12文字程度で切られる
    expect(manifest.short_name.length).toBeLessThanOrEqual(12);
  });
});
```

- [ ] **Step 5: テストを実行して失敗を確認する**

Run: `npm run test:run -- src/pwa/manifest.test.ts`
Expected: PASS（7件）。Step 3 で `manifest.ts` を先に作っているため、ここは赤にならない。設定値の宣言に対するテストなので、実装を先に書く形で構わない。

- [ ] **Step 6: `vite.config.ts` に PWA を組み込む**

**この設定がこのタスクの中心。** `globPatterns` にフォントを含めないことが設計の要である。

```ts
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import { VitePWA } from "vite-plugin-pwa";
import { manifest } from "./src/pwa/manifest";

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      // 自動で切り替えない。スキャン中に画面が変わると、
      // 記録できたのか分からなくなる。
      registerType: "prompt",
      manifest,
      workbox: {
        /*
          プリキャッシュにフォント実体を含めない。

          日本語フォント3種は unicode-range で496個に分割されており、
          全部で 11.5MB ある。すべてプリキャッシュすると初回に25MBを
          要求することになり、教室のWi-Fiでは落とせない。

          ブラウザは実際に表示する文字を含むサブセットしか取りに
          行かないので、フォントは実行時キャッシュ（下の
          runtimeCaching）に委ねる。初回は約920KBで起動する。

          この配列に "**/*.woff2" を足してはいけない。
        */
        globPatterns: ["**/*.{js,css,html}"],
        runtimeCaching: [
          {
            // 一度取ったフォントは端末に残す。次からはオフラインでも出る。
            urlPattern: /\.woff2$/,
            handler: "CacheFirst",
            options: {
              cacheName: "fonts",
              expiration: {
                maxEntries: 200,
                // 1年。フォントの中身は変わらない。
                maxAgeSeconds: 60 * 60 * 24 * 365,
              },
              cacheableResponse: { statuses: [0, 200] },
            },
          },
        ],
      },
    }),
  ],
  test: {
    environment: "jsdom",
    setupFiles: ["./src/test/setup.ts"],
    css: false,
  },
});
```

- [ ] **Step 7: ビルドしてプリキャッシュ量を確かめる**

Run: `npm run build`

続けて、生成された Service Worker が**フォントをプリキャッシュしていないこと**を確認する。

Run: `grep -c "woff2" dist/sw.js`
Expected: `0`。1以上ならフォントがプリキャッシュに入っている。`globPatterns` を確認すること。

Run: `ls -la dist/manifest.webmanifest dist/sw.js dist/_redirects`
Expected: 3ファイルとも存在する。

プリキャッシュの総量を測る。

```bash
node -e "
const fs=require('fs');
const sw=fs.readFileSync('dist/sw.js','utf-8');
const urls=[...sw.matchAll(/\"url\":\"([^\"]+)\"/g)].map(m=>m[1]);
let total=0;
for(const u of urls){
  const p='dist/'+u.replace(/^\//,'');
  if(fs.existsSync(p)) total+=fs.statSync(p).size;
}
console.log('プリキャッシュ:', urls.length, 'ファイル', (total/1024).toFixed(0)+'KB');
"
```

Expected: 約920KB。**2MBを超えていたらフォントが混ざっている。**

- [ ] **Step 8: 全テストと型チェックを通す**

Run: `npm run test:run && npm run build`
Expected: 276件すべてPASS、型エラーなし。

**`vite.config.ts` が `src/pwa/manifest.ts` を import するため、型チェックの対象に入る。** `tsconfig.json` の `include` に `vite.config.ts` が含まれていることは確認済み。

- [ ] **Step 9: コミット**

```bash
git add -A
git commit -m "feat: PWA化しHTTPS配信できるようにする"
```

---

### Task 4: 更新の知らせ

**Files:**
- Create: `src/components/UpdateBanner.tsx`
- Create: `src/pwa/useAppUpdate.ts`
- Modify: `src/App.tsx`
- Test: `src/components/UpdateBanner.test.tsx`

**Interfaces:**
- Produces:
  - `useAppUpdate(): { needsUpdate: boolean; apply: () => void }`
  - `<UpdateBanner />`

- [ ] **Step 1: 失敗するテストを書く**

`src/components/UpdateBanner.test.tsx`。**Service Worker は jsdom で動かない。** `virtual:pwa-register/react` をモックして、表示のロジックだけを試す。

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { beforeEach, describe, expect, it, vi } from "vitest";

const updateServiceWorker = vi.fn();
let needRefresh = false;

vi.mock("virtual:pwa-register/react", () => ({
  useRegisterSW: () => ({
    needRefresh: [needRefresh, vi.fn()],
    offlineReady: [false, vi.fn()],
    updateServiceWorker,
  }),
}));

const { UpdateBanner } = await import("./UpdateBanner");

beforeEach(() => {
  needRefresh = false;
  updateServiceWorker.mockClear();
});

describe("更新の帯", () => {
  it("新しい版が無ければ何も出さない", () => {
    needRefresh = false;
    const { container } = render(<UpdateBanner />);

    expect(container).toBeEmptyDOMElement();
  });

  it("新しい版があれば知らせる", () => {
    needRefresh = true;
    render(<UpdateBanner />);

    expect(screen.getByText("新しい版があります")).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "更新" })).toBeInTheDocument();
  });

  it("押したときだけ切り替える", async () => {
    const user = userEvent.setup();
    needRefresh = true;
    render(<UpdateBanner />);

    // 出しただけでは切り替えない。スキャン中に画面が変わると
    // 記録できたのか分からなくなる。
    expect(updateServiceWorker).not.toHaveBeenCalled();

    await user.click(screen.getByRole("button", { name: "更新" }));

    expect(updateServiceWorker).toHaveBeenCalledWith(true);
  });
});
```

- [ ] **Step 2: 仮想モジュールの型を通す**

`virtual:pwa-register/react` は `vite-plugin-pwa` が用意する仮想モジュールで、そのままだと TypeScript が解決できない。

`src/vite-env.d.ts` に足す（無ければ作る）。

```ts
/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />
```

- [ ] **Step 3: テストを実行して失敗を確認する**

Run: `npm run test:run -- src/components/UpdateBanner.test.tsx`
Expected: FAIL。`Failed to resolve import "./UpdateBanner"`

- [ ] **Step 4: 更新のフックを書く**

`src/pwa/useAppUpdate.ts`:

```ts
import { useRegisterSW } from "virtual:pwa-register/react";

/**
 * 新しい版が用意できたかを返す。
 *
 * 自動で適用しない。先生がスキャンしている最中に画面が切り替わると、
 * いま読んだカードが記録できたのか分からなくなる。
 */
export function useAppUpdate(): {
  needsUpdate: boolean;
  apply: () => void;
} {
  const {
    needRefresh: [needRefresh],
    updateServiceWorker,
  } = useRegisterSW();

  return {
    needsUpdate: needRefresh,
    apply: () => void updateServiceWorker(true),
  };
}
```

- [ ] **Step 5: 帯を書く**

`src/components/UpdateBanner.tsx`。**朱は使わない。** 朱は花丸と削除確認のためのもので、更新は破壊的な操作ではない。

```tsx
import { useAppUpdate } from "../pwa/useAppUpdate";

/** 新しい版が出たことを知らせる帯。押したときだけ切り替える。 */
export function UpdateBanner() {
  const update = useAppUpdate();

  if (!update.needsUpdate) {
    return null;
  }

  return (
    <div
      role="status"
      className="border-kogan bg-gayoshi fixed inset-x-0 bottom-0 z-20 flex items-center justify-between gap-3 border-t px-4 py-3"
    >
      <span className="text-sm font-bold">新しい版があります</span>
      <button
        type="button"
        onClick={update.apply}
        className="bg-ai min-h-11 shrink-0 rounded px-4 py-2 font-bold text-gayoshi"
      >
        更新
      </button>
    </div>
  );
}
```

- [ ] **Step 6: アプリに組み込む**

`src/App.tsx` の `App`（`AppRoutes` ではない）に足す。**`AppRoutes` に入れてはいけない。** テストが `AppRoutes` を `MemoryRouter` で包んで使っており、そこに Service Worker 依存が入ると全テストがモックを要求することになる。

```tsx
export function App() {
  return (
    <BrowserRouter>
      <AppRoutes />
      <UpdateBanner />
    </BrowserRouter>
  );
}
```

import を足す。

```tsx
import { UpdateBanner } from "./components/UpdateBanner";
```

- [ ] **Step 7: テストを実行して成功を確認する**

Run: `npm run test:run -- src/components/UpdateBanner.test.tsx`
Expected: PASS（3件）

- [ ] **Step 8: 全テストと型チェックを通す**

Run: `npm run test:run && npm run build`
Expected: 279件すべてPASS、型エラーなし。

**既存テストが `virtual:pwa-register/react` の解決に失敗する可能性がある。** その場合、`UpdateBanner` が `AppRoutes` ではなく `App` に入っているかを確認すること。既存テストは `AppRoutes` を使っている。

- [ ] **Step 9: 連続実行で安定を確認する**

Run: `npm run test:run`（3回連続）
Expected: 3回とも全件PASS。

- [ ] **Step 10: コミット**

```bash
git add -A
git commit -m "feat: 新しい版を知らせる帯を追加"
```

---

## 完了条件

- `npm run test:run` が全件PASSする（3回連続で確認）
- `npm run build` が型エラーなく通る
- `dist/sw.js` に `woff2` が1つも含まれない
- プリキャッシュの総量が約920KB（2MBを超えないこと）
- `dist/` に `manifest.webmanifest`、`sw.js`、`_redirects` が揃う

## 配信の手順（人が行う）

ビルドしてできた `dist/` を配信する。

**Cloudflare Pages の場合**
1. https://dash.cloudflare.com → Workers & Pages → Create → Pages → Upload assets
2. `dist` フォルダをドラッグ＆ドロップ
3. 発行されたURLを開く

**Netlify の場合**
1. https://app.netlify.com/drop を開く
2. `dist` フォルダをドラッグ＆ドロップ
3. 発行されたURLを開く

どちらも無料枠でHTTPSが付き、`_redirects` を読む。GitHubへの接続は不要。

## 手動確認が必要な項目

Service Worker とホーム画面への追加は自動テストできない。

- **HTTPSのURLを開いてカメラが起動すること。** これがこのステップの主目的
- ホーム画面に追加し、アイコンが花丸で表示されること
- アイコンから開いてアドレスバーが出ないこと（`display: standalone`）
- **機内モードにして開き直しても起動すること**
- 一度表示した生徒の氏名が、オフラインでも字が出ること
- **まだ表示していない字はオフラインで出ないこと**（設計上の割り切り。出席番号は出る）
- `/roster` を直接開いて404にならないこと
- 新しい版を配信したとき、帯が出て押すと切り替わること

**ステップ1〜3で溜まった未確認項目も、ここでまとめて確認できる。** `docs/手元での確認手順.md` を参照。

## 次のステップ

ステップ4（未提出者一覧・集計）またはステップ5（バックアップ）へ進む。実機で触った感触を設計に反映できる状態になっている。
