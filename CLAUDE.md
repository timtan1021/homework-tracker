# 宿題提出管理アプリ

小学5年1クラス（34人）の担任1名が、朝の会の前に宿題の提出をチェックするアプリ。オフラインで動き、データは端末内のIndexedDBに閉じる。

## 誰がどう使うか

先生は8:15の締切前、**タブレットを片手に持ち、もう片方でドリルを受け取りながら**操作する。読む時間はない。この前提が設計の多くを決めている。

- 主画面（名簿・スキャン）は説明文をほぼ持たない。番号とセルの状態が情報を運ぶ
- 出席番号は腕を伸ばした距離から読める大きさ（24pt以上）
- タップ領域は44px四方以上

## 現状

| ステップ | 内容 | 状態 |
|---|---|---|
| 1 | 生徒名簿・QRカード印刷 | 完了 |
| 2 | 提出物マスタ・締切・曜日 | 完了 |
| 3 | スキャン画面・複数選択 | 完了 |
| 4 | 未提出者一覧・集計・自動未提出確定 | 完了 |
| 5 | JSONバックアップ(書き出し・復元) | 完了 |
| 6 | PWA化・HTTPS配信 | 完了 |
| 7 | 未提出者一覧での欠席マーキング | 完了 |
| 8 | カレンダー画面・日付指定の宿題 | 完了 |
| 9 | 採点(合格・再提出) | 完了 |
| 10 | 教員/児童のページ分離・パスワード認証・合言葉による復旧 | 完了 |
| 11 | スキャン・児童用スキャン・採点・未提出者一覧の日付送り | 完了 |

**実機での確認が一度も行われていない。** `docs/手元での確認手順.md` に確認項目がある。

## コマンド

```
npm run test:run   # 単発実行
npm run build      # tsc --noEmit && vite build
npm run dev        # localhost:5173
```

## 譲れない制約

- **ネットワークアクセスを書かない。** `fetch`、CDN、外部URLは禁止。フォントは `@fontsource/*` でバンドル済み
- **配色は5トークンのみ**: 画用紙 `gayoshi` / 墨 `sumi` / 藍 `ai` / 方眼 `kogan` / 朱 `shu`。**Tailwind素のカラー（`text-white` 等）を使わない。** 藍の上に載せる明色は `text-gayoshi`
- **朱は花丸と、完全に元へ戻せない操作（完全削除・復元）の確認ダイアログだけ。** 枠線や小さい文字にまで朱を広げると「丸がついた」という記号が薄まる。朱の文字は18pt以上のみ（コントラスト比が約4.3:1のため）。範囲を広げるときは、既存の `ConfirmDialog` の `tone="danger"` を使うだけに留め、新しい仕組みを増やさない
- 書体: 数値は `font-num`（BIZ UDGothic）、見出しは `font-display`（Klee One）。**Klee One を本文に使わない**
- UIの文言はすべて日本語。エラー文は何が起きたかと次にどうするかを示す。**謝罪表現を使わない**
- TypeScript `strict`。`any` を使わない。`noUnusedLocals: true` なので未使用importでビルドが落ちる
- 375px幅で崩れない。`prefers-reduced-motion` を尊重する

## 設計判断とその理由

読み返したとき「なぜこうなっているか」が分からなくなりやすいもの。

- **QRには出席番号ではなく内部ID（uuid）を埋める。** カードは印刷・ラミネート済みで刷り直せない。番号を修正してもQRは有効なまま
  - ただし**カードには番号も印字されている**ため、番号を変えると印刷済みカードの表示だけがずれる。未解決の課題
- **転出した生徒の出席番号は欠番として残す。** 詰め直すと印刷済みカードと名簿がずれる。「完全に削除」は別操作
- **提出物の `deadline` は `"HH:mm"` 文字列。** `Date` だと日付が付いて「毎日この時刻」を表せない
- **提出記録の `date` は `"YYYY-MM-DD"` をローカルで切る。** `toISOString()` を使うとUTC変換で日本時間の朝9時前が前日になる
- **未提出のレコードは作らない。** 「記録が無い＝未提出」で表す。34人×提出物×日数を毎日作るのは無駄で、締切前と未提出の区別もつかない
- **`by-unique` インデックスで二重記録を防ぐ。** アプリのロジックだけだとカメラの連写や二重タップで抜ける
- **カメラは HTTPS 必須。** `getUserMedia` はセキュアコンテキスト限定でポリフィルが存在しない。だから「番号でチェック」は退避手段ではなく同格の入力手段として常設している

## 踏んだ落とし穴（同じ轍を踏まないこと）

### テストの競合を6回踏んでいる

すべて同じ形。**非同期に変わる内容を1回だけ見て判定している。**

- `findBy*` が待つのは**要素の出現**であって**中身の更新**ではない。要素が先に存在する場合（プレースホルダなど）、中身が入る前に判定が走る
- 既存要素の属性を見るときは `waitFor` で囲む
- 再描画で要素が差し替わるため、掴んだDOM参照を使い回さない。毎回引き直す
- `clear()` + `type()` は負荷下で競合する。`fireEvent.change` を使う

### 時計を偽装してはいけない

`vi.useFakeTimers` は二重に壊す。

- タイマーまで偽装すると **fake-indexeddb の内部スケジューリングが止まり**、トランザクションが `InvalidStateError` で落ちる
- `toFake: ["Date"]` に絞っても、**`Date.now()` の凍結で Testing Library の `waitFor` が経過時間を測れなくなる**

日付に依存するテストは、実際の今日から相対的に組み立てる。

### DBのバージョン移行

`upgrade` は必ず `oldVersion` で分岐する。まとめて作ると、既にストアがある端末で `createObjectStore` が例外を投げ、**名簿ごと開けなくなる**。

移行を足すときは、旧バージョンのDBを手で作ってから開き直し、**既存データが残ることを検証するテストを先に書く**。バックアップ（ステップ5）があるので万一のときは復元できるが、移行そのものの正しさを保証するものではない。

### `put()` は失敗すると同期的に例外を投げる

IndexedDBの `put()` は、値がストアの `keyPath`（このプロジェクトでは常に `id`）を満たさないと、Promiseではなく**その場で同期的に例外を投げる**。

複数の `put()` を配列にまとめて `Promise.all([...])` に渡す書き方（`restoreBackup` がこれをやっていた）は危険。配列リテラルの評価は`Promise.all`が呼ばれる**前に**左から順に走るため、途中の項目で同期例外が起きると、それより前の `put()` はすでにトランザクションへ積まれた状態のまま `Promise.all` 自体が呼ばれずに関数が中断する。積まれた分はキャンセルされず、トランザクションはそのままコミットされる。「一部だけ書き込まれて残りが消える」という、エラーハンドリングでは防げない部分書き込みになる（バックアップ復元で実際に見つけた）。

対策は書き込みの前に全項目を検証すること。書き込みが始まってからの `try/catch` では遅い。

### props同期の `useEffect` を置かない

`useState(defaultValue)` の下に `useEffect(() => setState(defaultValue), [defaultValue])` を置くと、**初回描画から effect 実行までの間の入力を巻き戻す**。値を作り直したいときは呼び出し側が `key` を変える。

### 寸法は必ず式で書き、計算し直せるようにする

印刷レイアウトでA4の印字領域ぴったりに余白を取り、その上にページ見出しを載せて**全ページが1枚に切り詰められる**不具合を出した。カード寸法・行数・列数を変えるときは式を計算し直す。CSSと定数の二重管理になる箇所は、テストから `index.css` を読んで突き合わせている。

### バージョン番号を固定するテストを書かない

`expect(DB_VERSION).toBe(2)` はバージョンを上げるたびに壊れる。そのテストが守りたいのは番号ではなくストアの構成。

## 進め方

このプロジェクトでは次の手順が効いている。

1. **ブレインストーミング → 設計書 → 実装計画 → 実装**（superpowers のスキル）
2. **実装計画を書いたら、コードを一度リポジトリへ適用して動かす。** 毎回3〜4件の欠陥（未使用import、型の広がり、テストの競合、実バグ）が出る。確認したら巻き戻し、TDDでやり直す
3. **各タスクで「わざと壊してテストが落ちること」を確認する。** テストが通ったという事実だけでは、それが何かを守れている証拠にならない
4. 実装後に制約違反を機械的に検査する（朱の使用箇所、Tailwind素のカラー、`any`、タップ領域）

### サブエージェントについて

ステップ1では「タスクごとに新しいエージェント＋独立レビュー」で進め、最終レビューが致命的な不具合（印刷が1枚に切り詰められる）を捕まえた。ステップ2〜4の実装中はエージェント起動が権限分類器にブロックされ続け、独立レビューが入らないまま進んだ。ステップ4の完了後に改めて試したところ起動できるようになっており、既存コード（ステップ4〜6）に対する独立レビューで `restoreBackup` の部分書き込みバグを含む実際の欠陥を見つけた。**ブロックされているように見えても、区切りごとに起動を試すこと。** 一度失敗したからといって以降ずっと避ける理由にはならない。

## 作業記録

- `docs/superpowers/specs/` — 設計書。判断の理由が書いてある
- `docs/superpowers/plans/` — 実装計画。コードそのものが載っている
- `docs/手元での確認手順.md` — 人の目でしか確かめられない項目
- `.superpowers/sdd/` — 各ステップの進行記録（gitには含めない）
